---
// Import the necessary AccordionItem component
import AccordionItem from "@components/ui/blocks/AccordionItem.astro";

// Definimos los datos directamente aquí (Hardcoded)
const title = "Resolvemos tus dudas sobre <span class='text-red-600 dark:text-red-500'>proyectos y servicios</span>";

const faqsData = {
  subTitle: "¿Tienes dudas sobre nuestros servicios? Aquí respondemos las consultas habituales de nuestros clientes industriales y navales.",
  faqs: [
    {
      question: "¿En qué zonas de la región de Magallanes prestan servicios?",
      answer:
        "Operamos en toda la región, incluyendo Punta Arenas, Puerto Natales, Porvenir y Puerto Williams. Contamos con la logística necesaria para trasladarnos a zonas remotas, pontones de acuicultura y realizar trabajos a bordo de embarcaciones en tránsito.",
    },
    {
      question: "¿Realizan proyectos de automatización a medida?",
      answer:
        "Sí. Desarrollamos soluciones personalizadas que van desde la programación de PLC y tableros de control, hasta sistemas complejos de monitoreo remoto (IoT), integración de redes NMEA2000 para barcos y sistemas SCADA para plantas de proceso.",
    },
    {
      question: "¿Qué tipo de mantenimiento ofrecen para evitar paradas?",
      answer:
        "Ofrecemos mantenimiento preventivo y correctivo (Nivel 1 y 2). Utilizamos tecnología avanzada como termografía cualitativa y cuantitativa, análisis de redes y medición de aislamiento para detectar fallas antes de que afecten tu operación.",
    },
    {
      question: "¿Atienden emergencias fuera de horario?",
      answer:
        "Sí, entendemos que la industria no se detiene. Nuestros clientes con planes de mantenimiento activos (Plan Nexus y Plan Pro) cuentan con soporte prioritario y asistencia técnica dedicada para resolver incidencias críticas lo antes posible.",
    },
    {
      question: "¿Sus instalaciones cumplen con la normativa SEC?",
      answer:
        "Absolutamente. Todos nuestros proyectos se diseñan y ejecutan bajo estricto cumplimiento de la normativa eléctrica chilena vigente (SEC). Además, en proyectos navales, nos regimos por los estándares internacionales de seguridad marítima.",
    },
  ],
};

// Define a helper function to generate ids dynamically.
const makeId = (base: any, index: any) => `${base}${index + 1}`;
---

<section
  class="mx-auto max-w-[85rem] px-4 py-10 sm:px-6 lg:px-8 lg:py-14 2xl:max-w-full"
>
  <div class="grid gap-10 md:grid-cols-5">
    <div class="md:col-span-2">
      <div class="max-w-xs">
        <h2
          class="text-2xl font-bold text-neutral-800 dark:text-neutral-200 md:text-4xl md:leading-tight"
        >
          <Fragment set:html={title} />
        </h2>
        <p class="mt-1 hidden text-neutral-600 dark:text-neutral-400 md:block">
          {faqsData.subTitle}
        </p>
      </div>
    </div>
    <div class="md:col-span-3">
      <div
        class="hs-accordion-group divide-y divide-neutral-200 dark:divide-neutral-700"
      >
        {
          faqsData.faqs.map((question, i) => {
            // Generate ids dynamically for each FAQ accordion item.
            let id = makeId(
              "hs-basic-with-title-and-arrow-stretched-heading-",
              i
            );
            let collapseId = makeId(
              "hs-basic-with-title-and-arrow-stretched-collapse",
              i
            );

            return (
              <AccordionItem
                {...question}
                id={id}
                collapseId={collapseId}
                first={i === 0}
              />
            );
          })
        }
      </div>
    </div>
  </div>
</section>